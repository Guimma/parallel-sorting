#ifndef SORTLIB_H
#define SORTLIB_H
#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <stdbool.h>
#include <math.h>

int* fillArray(int *array, int size);
void printArray(int *array, int size);
int getLargest(int *array, int size);

#endif